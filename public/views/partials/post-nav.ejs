    </ul>
    <ul class="nav navbar-nav navbar-right nav-soften">
      <% if (user) { %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <span class="glyphicon glyphicon-info-sign"></span>
              <% if (!user.username) { %>
                <% if (!user.name) { %>
                  user
                <% } else { %>
                  <%= user.name %>
                <% } %>
              <% } else { %>
                <%= user.username %>
              <% } %>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="/user">User Settings</a></li>
            <li class="divider"></li>
            <li><a href="/auth/logout">Logout</a></li>
          </ul>
        </li>
      <% } else { %>
        <li class="active">
          <a href="/login"><span class='nav-text-bordered'>Login</span></a>
        </li>
      <% } %>
    </ul>
  </div>
</nav>
<div id="flash" class="alert"></div>
<div class="header-buffer"></div>
<img id='collapse-button' class='hide-desktop-nav' src="/images/collapse-arrow.svg">
<a href='#'><img id='button-scroll-to-top' class='flip-image' src="/images/collapse-arrow.svg"></a>

<script>
var header_collapse = new CollapseManager('nav-container', 'mobile-header-title');
header_collapse.setTrigger('collapse-button');
</script>
